COMPILING
Using the attached makefile, chomp.cpp should compile and run without any issues.
Simply type: 'make'

RUNNING CHOMPCOUNT
Our program relies on the use of an input file ‘input.txt’ 
and writes its output to a new file ‘output.txt’
After compiling, simply type: './chompcount'
